import React, { Fragment, Component } from "react"

class EventReport extends Component {
  render() {
    //const { idGroup, idEvent } = this.props.params.match

    return (
      <Fragment>
        <h2>Resultado do Evento</h2>
      </Fragment>
    )
  }
}

export default EventReport
