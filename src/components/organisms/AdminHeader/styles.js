import styled from "styled-components"

export const LoggedHeader = styled.header`
  background: #fff;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-height: 54px;
  height: 55px;
  min-height: 54px;
`

export const RightMenu = styled.div`
  display: flex;
  align-items: center;
`
