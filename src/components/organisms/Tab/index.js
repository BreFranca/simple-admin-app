// import React from 'react';
// import PropTypes from 'prop-types';
// import cx from 'classnames';

// import { FontIcon } from "../../atoms/FontIcon"

// import { Container } from "./styles"

// const Tab = (props) => {

//   const { className, label, icon, onClick } = props;

//   const cssTabClass = cx('tabs__tab', {
//     // ['tabs__tab-link--active']: isActive
//   });

//   const activeLinkClass = cx('tabs__tab-link', className);

//   return (
//     <Container className={cssTabClass} onClick={onClick}>
//       {/* <a href="#" className={activeLinkClass}>
//         <FontIcon name={icon} /> 
//         {label}
//       </a> */}
//     </Container>
//   );
// };

// Tab.propTypes = {
//   className: PropTypes.string,
//   label: PropTypes.string.isRequired,
//   icon: PropTypes.string,
//   isActive: PropTypes.bool,
//   onClick: PropTypes.func
// };

// export default Tab;
